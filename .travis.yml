Error: Can't find a root directory while resolving a config file path.language: node_js
node_js:
  - "10.6"
before_install:
  - curl - o - -L https://yarnpkg.com/install.sh | bash -s -- --version 1.9.2
  - export PATH="$HOME/.yarn/bin:$PATH"
